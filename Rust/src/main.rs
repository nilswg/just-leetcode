mod problems;

fn main() {
    problems::test_solutions();

    // for i in 0..16 {

    //         let val = char::from_digit((i + 1) as u32, 16).unwrap();

    //         println!("{}", &val)
    // }
}
